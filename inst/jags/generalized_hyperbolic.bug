model
{
	for (i in 1:N)
	{
		u2[i] <- X2[i] * pow(alpha, T2[i])
		u1[i] <- X1[i] * pow(alpha, T1[i])

		logit(p[i]) <- a * (u2[i] - u1[i])

		C[i] ~ dbern(p[i])
	}

	a ~ dunif(0.5, 1.5)
	alpha ~ dunif(0.01, 0.99)
	gamma ~ dunif(0.01, 0.99)
}
