model
{
	for (i in 1:N)
	{
		u2[i] <- X2[i] / (1 + k * T2[i])
		u1[i] <- X1[i] / (1 + k * T1[i])

		logit(p[i]) <- a * (u2[i] - u1[i])

		C[i] ~ dbern(p[i])
	}

	a ~ dunif(0.5, 1.5)
	k ~ dunif(0.01, 10.00)
}
